export enum ExpenseStaus {
    PAGADO = 'PAGADO',
    PENDIENTE = 'PENDIENTE',
    CANCELADO = 'CANCELADO'
}

export const ExpenseStatusList = [
    ExpenseStaus.PAGADO,
    ExpenseStaus.PENDIENTE,
    ExpenseStaus.CANCELADO
]