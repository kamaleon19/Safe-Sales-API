export enum PaymentMethod {
    EFECTIVO = 'EFECTIVO',
    DEBITO = 'DEBITO',
    CREDITO = 'CREDITO',
    TRANSFRENCIA = 'TRANSFERENCIA',
    QR = 'QR'
}

export const PaymentMethodList = [
    PaymentMethod.EFECTIVO,
    PaymentMethod.DEBITO,
    PaymentMethod.CREDITO,
    PaymentMethod.TRANSFRENCIA,
    PaymentMethod.QR,
]